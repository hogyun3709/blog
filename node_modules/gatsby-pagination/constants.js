"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
/**
 * Constants for Gatsby-Pagination
 * @private
 * @module constants
 * @type {Object}
 */

/**
 * The limit which gatsby-paginate will default to in createPaginatedPages.
 *
 * @private
 * @constant
 * @type {number}
 * @default 10
 *
 * @see createPaginationPages
 */
var DEFAULT_LIMIT = exports.DEFAULT_LIMIT = 10;

/**
 * The default path formatter generates a relative path starting with "/", use by createPaginatedPages.
 *
 * @private
 * @constant
 * @type {function}
 * @default
 *
 * @see createPaginationPages
 */
var DEFAULT_PATH_FORMATTER = exports.DEFAULT_PATH_FORMATTER = function DEFAULT_PATH_FORMATTER(pageNumber) {
  return pageNumber === 1 ? "/" : "/" + pageNumber;
};

exports.default = {
  DEFAULT_LIMIT: DEFAULT_LIMIT,
  DEFAULT_PATH_FORMATTER: DEFAULT_PATH_FORMATTER
};